[pytest]
testpaths = tests
pythonpath = src/GPU
addopts = -ra --strict-markers
xfail_strict = true
markers =
    known_bug: executable regression for a documented, unfixed defect
    policy: characterization of an existing search policy, not a correctness requirement
